You are optimizing GrhSIM's end-to-end XiangShan SimTop simulation performance.

Research the repository deeply before proposing one candidate. Read the current
implementation, generated GrhSIM and GSIM C++, prior experiments in
`pdocs/grhsim_opt` and `pdocs/grhsim_opt_thj`, and available perf/emu artifacts.
Use concrete evidence; do not repeat a direction that the records already close
unless you identify materially new evidence or a different mechanism.

Keep repository inspection bounded to the target checkout that is already the
working directory. Prefer `rg`, `rg --files`, and exact paths named by the task
or prior records. Never recursively scan `/nfs/home`, the workspace parent,
`/tmp`, evaluator slots, or the SimpleTES checkpoint tree, and do not use an
unbounded `find` outside this repository. If `find` is genuinely needed, bind
it to an exact repository subdirectory and a small `-maxdepth`. Inspection is
read-only: do not probe writability or create, touch, move, or delete files.

The only optimization objective and final score is SimTop 50k `Host time spent`
walltime. Cycles, instructions, generated-source size, ELF sections, BAE, DAG
edges, PMU counters, and profiles are diagnostic evidence only. A change is
useful only if the fixed-ASLR, functionally equivalent end-to-end run improves.

This is the fresh post-four-positive baseline at parent
`de37459cdd210794fa5d7423e6f32c145cf71261` and Wolvrix
`fd12d83f5150cc98540ed3e8f2af3b79f8054da0`. R (cold register-guard
admission and group hints), W (singleton MemoryWritePort hints inside an
R-admitted run), and A (adjacent SystemTask plus `xs_assert_v2` outer-guard
nesting) are already native-default behavior. Do not propose, reconstruct, or
claim any of R/W/A again. Four additional mechanisms are also native-default:
cold SystemTask/eligible standalone `xs_assert_v2` guards, always-inline hot
word helpers, in-range constant MemoryRead row proof with its redundant zero
pass removed, and cold dynamic scalar-shift/index bounds fallbacks. Do not
repropose any of those mechanisms. The MemoryFill F tier, residual dynamic
MemoryRead hint, and physical zero-tail mechanism did not land and are closed
prior directions. Research a distinct mechanism.

Return exactly one JSON object matching `candidate.schema.json`. The backend
will place it between the EVOLVE-BLOCK markers. Its fields mean:

- `schema_version`: always `2`.
- `candidate_mode`: one of `default-path` or `explicit-options` for every newly
  generated candidate. `control` is reserved for the initial no-op seed.
- `hypothesis`: the precise mechanism and expected end-to-end effect.
- `evidence`: repository paths plus absolute observed counts/timings/profile
  facts that motivated this candidate. Never invent measurements.
- `patch`: a complete `git diff`-style unified diff relative to
  `wolvrix@fd12d83f5150cc98540ed3e8f2af3b79f8054da0`. It must end with a
  newline.
- `enable_options`: an array of `{ "name": snake_case, "value": ... }` entries.
  It must be empty for `default-path` and non-empty for `explicit-options`.
  Names map to `WOLVRIX_XS_GRHSIM_<UPPER_CASE_NAME>` and must be members of the
  explicit optimization allowlist in `candidate.schema.json`. Reuse an existing
  default-off experimental policy when refining its implementation. Diagnostic
  `probe`/profile/report/stats/trace/dump/export values are forbidden.

Choose the attribution mode from the code path you are changing:

- `default-path`: use a non-empty patch and empty `enable_options` when refining
  behavior reached by the pinned native C++ defaults. The evaluator builds the
  patched tree with native options `{}` and requires its generated C++/headers
  to differ from the unpatched current-default control.
- `explicit-options`: use both a non-empty patch and non-empty `enable_options`
  when refining a genuinely default-off policy. The evaluator requires the
  patched no-options build to equal control, and requires the enabled output to
  differ from both control and an unpatched same-options emit.

`active_mask_gap_pack_policy=targeted-direct` controls only its non-table direct
active-mask gap-packing transformation. It is not an umbrella gate for exact
event/commit optimizations. `commit_exact_event_policy` is the independent
control for that mechanism; when its optimized behavior is already the native
default, refine it with `candidate_mode=default-path`, not by borrowing
`targeted-direct`.

Hard constraints:

1. Modify only existing, tracked, regular C/C++ source/header files under
   `wolvrix/lib/`, `wolvrix/include/`, or `wolvrix/app/pybind/`, with suffix
   `.c`, `.cc`, `.cpp`, `.cxx`, `.h`, `.hh`, `.hpp`, `.hxx`, or `.inc`. Do not
   add, delete, or rename files. Diff paths omit the leading `wolvrix/` because
   the patch is applied inside that submodule.
2. Never modify the benchmark, runner, workload, timing output, GSIM reference,
   generated files, parent-repository scripts, documentation, submodule
   pointers, or external dependencies. A `default-path` patch may deliberately
   change current native-default generation only through the allowed wolvrix
   source files and is always measured against the pinned unpatched control.
3. Declare attribution exactly. `default-path` requires patch/non-empty and
   options/empty. `explicit-options` requires both patch and options non-empty
   and must remain byte-identical to control with options absent. `control`
   requires both empty and is forbidden for generated proposals. The evaluator
   cross-checks these shapes rather than trusting the mode label.
4. Do not use absolute paths, path traversal, symlinks, executable/binary diffs,
   rename/copy directives, shell fragments, or credentials. Do not read, quote,
   expose, or place any API key/authentication material in the response.
   Resume-from, stats, profile, stop, export, instrumentation, and measurement
   controls are evaluator-owned and cannot be selected through `enable_options`.
5. Preserve simulator semantics. The evaluator performs focused tests,
   fixed-ASLR 100/10k gates, and a strict 50k functional/performance protocol.
   It rejects invalid infrastructure samples rather than treating them as a
   performance result.
6. Prefer a small, attributable candidate. Do not bundle unrelated ideas.
7. Treat the landed R/W/A implementation, the four landed hot-path mechanisms,
   and the rejected F/residual-MemoryRead/physical-zero-tail mechanisms as
   immutable baseline history. A patch that repeats any of them is invalid,
   even if it renames the policy or moves the same logic elsewhere.

The first program in this post-four-positive namespace is the schema-v2 special no-op
`control`. A later fresh continuation may use a previously evaluated best
candidate only when its checkpoint records these exact new parent/Wolvrix
pins. Never resume from or seed with an older `d31118b`/`16a9f49` or pre-RWA
`fbe4e1c`/`8f6ba14` checkpoint; it is relative to a different baseline. Regardless of the valid seed, every
newly generated candidate must contain a non-empty patch and select exactly one
of the two non-control modes above.
